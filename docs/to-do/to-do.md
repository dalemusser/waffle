# WAFFLE Documentation To-Do

A list of documentation topics, guides, and example pages to create in future passes.

## 📚 Future Documentation & Example Topics

- **Migrations setup** (e.g., using `golang-migrate/migrate`)
- **Multiple databases in one DBDeps**
- **Testing DBDeps with mocks / dependency injection**
- **Shared route prefixes** for large features
- **API versioning** (`/v1`, `/v2`, etc.)
- **JSON API patterns** (handlers, encoding, error responses)
- **HTML template rendering** using Go templates in WAFFLE
- **WebSocket endpoints** (chat, notifications, real-time updates)
- **High-security admin panels** (admin auth, roles, middleware patterns)

## Notes
This file tracks ideas we do not want to lose while focusing on top‑level documentation first.
